所有的子模块都在data中,

访问首页(即bigpipe.html)时,传完首页接着同时传header2和footer模块(非ajax,与首页在同一个响应请求中),其它模块是滚动到了才用ajax请求

加快首屏性能,可以将头几个模块都用bigpipe技术,这场的弊端是,用户刷新时,头几个块是不用第一时间传过去的,所以实验中暂只传head和foot.



//已发现问题:
       //如果块的预设高度大于屏高 出现不加载的判断 及横向滚动条的闪现 
       //高度与快速滑动问题 块设得小会瞬间加载多页
       //刷新问题 所在scrollTop固定,但刷新后块高变为预设(通常比动态小),以至一下子就到了页底,加载末几个块 --刷新问题的技术方向应该是利用缓存机制
       
       //1px需要触发
